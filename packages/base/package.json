{
  "name": "@trackproof/base",
  "version": "0.0.0",
  "description": "TrackProof Base adapter: on-chain AnchorStore over the deployed Anchor contract (viem).",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "!dist/**/*.test.*"
  ],
  "dependencies": {
    "@trackproof/core": "0.0.0",
    "viem": "^2.21.0"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "node --test \"dist/**/*.test.js\""
  }
}
